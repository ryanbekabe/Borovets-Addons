# The default syntax is used for files that do not match any other syntax

syntax default
comment "#"

color cyan "^[[:space:]]*#.*"
color ,red " +	+"
color brightred "(GNU )?[Nn]ano [1-4]\.[0-9][-.[:alnum:]]*\>"
color magenta "<[[:alnum:].%_+-]+@[[:alnum:].-]+\.[[:alpha:]]{2,}>"
